/**
 * src/components/ui/Button.tsx
 * ---------------------------------------------------------------------------
 * Alias di AppButton per compatibilità con i file di scaffold esistenti
 * (SubjectsScreen, ChapterDetailScreen) che importano `Button` da qui.
 * ---------------------------------------------------------------------------
 */

export { AppButton as Button } from './AppButton';
